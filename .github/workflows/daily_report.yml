name: Global Market Monitor Parallel

on:
  schedule:
    - cron: '30 10 * * 1-5'
  workflow_dispatch:
    inputs:
      market_choice:
        description: 'Select Market'
        required: true
        default: 'all'
        type: choice
        options: [all, tw-share, us-share, hk-share, cn-share, jp-share, kr-share]

jobs:
  global-market-analysis:
    name: ${{ matrix.market.emoji }} ${{ matrix.market.name }} Analysis
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        market:
          - { id: "tw-share", name: "Taiwan", emoji: "🇹🇼", suffix: "tw" }
          - { id: "us-share", name: "US",     emoji: "🇺🇸", suffix: "us" }

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Environment Setup
        run: |
          python -m pip install --upgrade pip
          pip install pandas yfinance requests lxml tqdm resend matplotlib numpy

      - name: Download and Run Market Analysis
        env:
          # 💡 重要：確保這些 Secret 在 GitHub Settings 中已設定
          EMAIL_USER: ${{ secrets.EMAIL_USER }} 
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
        run: |
          # 1. 執行下載
          python "downloader_${{ matrix.market.suffix }}.py" --market ${{ matrix.market.id }}
          # 2. 執行分析與發信
          python main.py --market ${{ matrix.market.id }}
