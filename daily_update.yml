name: Predator Daily Auto-Update

on:
  schedule:
    - cron: '30 0 * * *' # 台北 08:30
    - cron: '30 8 * * *' # 台北 16:30
  workflow_dispatch:

jobs:
  market-analysis:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 🚀 強制環境對齊與安裝
        run: |
          # 1. 強制升級 pip 並在「當前路徑」安裝所有套件
          python -m pip install --upgrade pip
          python -m pip install streamlit streamlit-autorefresh pandas yfinance matplotlib requests beautifulsoup4 flask akshare pykrx
          
          # 2. 萬一環境隔離，我們強制把安裝路徑印出來檢查
          python -c "import streamlit; print('✅ Streamlit 安裝成功，版本:', streamlit.__version__)"

      - name: 🦅 執行分析程式
        run: |
          # 使用 python -m streamlit 或直接 python 執行
          # 這裡我們用 python 直接跑，確保它抓到剛才安裝的套件
          python main.py
        
      - name: 📦 更新數據至雲端
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add *.csv || echo "No CSV files"
          git commit -m "Auto-update market data [skip ci]" || echo "No changes"
          git push
