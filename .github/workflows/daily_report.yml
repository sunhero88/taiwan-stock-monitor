name: Global Market Monitor Parallel

on:
  schedule:
    - cron: '30 10 * * 1-5'
  workflow_dispatch:
    inputs:
      market_choice:
        description: 'Select Market'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - tw-share
          - us-share
          - hk-share
          - cn-share
          - jp-share
          - kr-share

jobs:
  global-market-analysis:
    name: ${{ matrix.market.emoji }} ${{ matrix.market.name }} Analysis
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        market:
          - { id: "tw-share", name: "Taiwan", emoji: "🇹🇼" }
          - { id: "us-share", name: "US",     emoji: "🇺🇸" }
          - { id: "hk-share", name: "HK",     emoji: "🇭🇰" }
          - { id: "cn-share", name: "China",  emoji: "🇨🇳" }
          - { id: "jp-share", name: "Japan",  emoji: "🇯🇵" }
          - { id: "kr-share", name: "Korea",  emoji: "🇰🇷" }

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Environment Setup
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk
          python -m pip install --upgrade pip
          pip install pandas yfinance requests lxml tqdm resend matplotlib numpy xlrd pykrx tokyo-stock-exchange akshare openai

      - name: Run Market Analysis
        env:
          # 💡 這裡必須對應 main.py 讀取的變數名稱
          EMAIL_USER: ${{ secrets.EMAIL_USER }}   # 收件人 Gmail
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}   # Resend API Key (re_...)
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python main.py --market ${{ matrix.market.id }}
