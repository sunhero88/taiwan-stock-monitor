name: Global Market Monitor Parallel

on:
  schedule:
    # 每個工作日台灣時間下午 6:30 執行 (UTC 10:30)
    - cron: '30 10 * * 1-5'
  workflow_dispatch:
    inputs:
      market_choice:
        description: '選擇要執行的市場 (Select Market)'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - tw-share
          - us-share

jobs:
  global-market-analysis:
    name: ${{ matrix.market.emoji }} ${{ matrix.market.name }} 分析
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        market:
          # suffix 必須對應你的檔名：downloader_tw.py / downloader_us.py
          - { id: "tw-share", name: "Taiwan", emoji: "🇹🇼", suffix: "tw" }
          - { id: "us-share", name: "US",     emoji: "🇺🇸", suffix: "us" }

    steps:
      - name: 檢出代碼 (Checkout Code)
        uses: actions/checkout@v4

      - name: 設定 Python 環境 (Set up Python)
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 安裝必要套件 (Environment Setup)
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk
          python -m pip install --upgrade pip
          # 一次性安裝所有數據分析與發信用套件
          pip install pandas yfinance requests lxml tqdm resend matplotlib numpy openai

      - name: 執行下載、分析與發信 (Download & Run)
        # 邏輯：只有在排程觸發、全選、或選中特定市場時才執行
        if: |
          github.event_name == 'schedule' || 
          github.event.inputs.market_choice == 'all' || 
          github.event.inputs.market_choice == matrix.market.id
        env:
          # 這裡必須對應 GitHub Settings > Secrets 裡的名稱
          EMAIL_USER: ${{ secrets.EMAIL_USER }}   # 你的收件 Gmail
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}   # Resend 的 re_ 開頭 API Key
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          # 1. 下載數據 (補上 --market 參數，確保 downloader 知道要存到哪)
          echo "📥 正在下載 ${{ matrix.market.name }} 數據..."
          python "downloader_${{ matrix.market.suffix }}.py" --market ${{ matrix.market.id }}
          
          # 2. 執行分析並發信 (這裡會去 data/${{ matrix.market.id }}/dayK 找 CSV)
          echo "📊 正在分析 ${{ matrix.market.name }} 並發送郵件..."
          python main.py --market ${{ matrix.market.id }}

      - name: 上傳分析數據 (選填：除錯用)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: market-data-${{ matrix.market.id }}
          path: data/${{ matrix.market.id }}/dayK/*.csv
          retention-days: 1
