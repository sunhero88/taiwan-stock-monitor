name: Predator Daily Analysis V14.0

on:
  schedule:
    - cron: '30 0 * * 1-5' # 台北 08:30
    - cron: '0 3 * * 1-5'  # 台北 11:00
    - cron: '30 8 * * 1-5' # 台北 16:30
  workflow_dispatch:      # 允許手動觸發

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' # 🚀 必須使用 3.10 以上版本，避免語法相容性報錯

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas yfinance numpy requests beautifulsoup4 streamlit resend matplotlib flask

      - name: Run Predator Analysis
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          REPORT_RECEIVER_EMAIL: "sunhero88@gmail.com"
        run: |
          python main.py --cli
